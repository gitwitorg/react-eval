{
  "name": "gitwit-data-analyser",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "ts-node generate.ts",
    "evaluate": "ts-node evaluate.ts"
  },
  "dependencies": {
    "@e2b/sdk": "^0.11.17",
    "dotenv": "^16.3.1",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/node": "^20.10.5"
  }
}
